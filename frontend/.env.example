# Which Midnight network the app talks to: "preview" or "preprod".
VITE_NETWORK=preview

# The one universal contract address every user connects to — deployed
# once via `npm run deploy:preview` (or deploy:preprod) in cli/.
# VITE_CONTRACT_ADDRESS=
